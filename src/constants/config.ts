const config = {
  baseUrl: 'https://phimapi.com',
  baseUrlFilter: 'https://phimapi.com/v1/api'
} as const

export const theme = {
  dark: 'dark',
  light: 'light',
  system: 'system'
} as const

export default config
